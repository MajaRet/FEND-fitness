import React from 'react';
import styled from 'styled-components';

const RepeatedExercise = ({ className }) => {
  return <div className={className}>RepeatedExercise</div>;
};

export default styled(RepeatedExercise)``;
