import React from 'react';
import styled from 'styled-components';

const TimedExercise = ({ className }) => {
  return <div className={className}>TimedExercise</div>;
};

export default styled(TimedExercise);
